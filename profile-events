#!/bin/bash
set -eo pipefail

cabal build profiling --ghc-options="-eventlog"
cd dist
build/profiling/profiling +RTS -N3 -ls -A256m -H512m -RTS
ghc-events-analyze --timed --bucket-width 1 --bucket-height 14 --ms --tick-every 10 --buckets 1000 --window Session profiling.eventlog
open profiling.0.timed.svg
threadscope profiling.eventlog
